# Feature Schema SQL

Use this folder for model-ready feature datasets in the `feature` schema.

- Feature SQL should derive inputs for churn, recommendations, forecasting, and segmentation.
- Keep feature contracts explicit and versionable.
- Training and inference jobs should both depend on these definitions.
